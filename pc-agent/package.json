{
  "name": "card-print-agent",
  "version": "1.0.0",
  "description": "Local Windows agent that receives print jobs from Card Print Studio (phone/browser) over the LAN and sends them directly to a connected printer, no print dialog.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pdf-lib": "^1.17.1",
    "pdf-to-printer": "^5.6.1"
  }
}
